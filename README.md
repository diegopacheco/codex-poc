# Coaching Backend

Start MySQL:

```
docker-compose up -d db
```

Run the server:

```
go run ./backend
```

API endpoints:

- `POST /members`
- `GET /members`
- `POST /teams`
- `GET /teams`
- `POST /assignments`
- `POST /feedbacks`
- `GET /feedbacks`
